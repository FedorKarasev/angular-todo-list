import { TodosTextFilterPipe } from './todos-text-filter.pipe';

describe('TodosTextFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TodosTextFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
